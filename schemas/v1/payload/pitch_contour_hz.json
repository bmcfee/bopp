{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "payload/pitch_contour.json",
  "title": "Pitch_Contour Payload",
  "type": "object",
  "properties": {
    "payload_type": {
      "const": "pitch_contour"
    },
    "values": {
      "$ref": "../core.json#/$defs/GenericBuffer",
      "description": "Pitch contours: (index, frequency, voicing)",
      "items": {
        "type": "object"
      }
    }
  },
  "required": [
    "payload_type",
    "values"
  ]
}
